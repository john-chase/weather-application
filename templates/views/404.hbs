<html>
<head>
    <title>{{title}}</title>
    <link rel="stylesheet" href="/css/styles.css" />
</head>
<body>
    {{>header}}
    <div class="content">
        <p>{{errorMsg}}</p><a href="{{route}}">Please try again</a>.
    </div>
    {{>footer}}
</body>
</html>